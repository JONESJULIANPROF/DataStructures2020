The ordered list merge in the orderedLinkedList.h file crashes on some computers. It has something to do with the version of Visual Studio and pointers.

There is an additional file called MergeSlow that works, but is slower to execute than the one in orderedLinkedList.h